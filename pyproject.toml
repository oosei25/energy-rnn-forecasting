[project]
name = "energy-rnn-forecasting"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pandas",
  "matplotlib",
  "scikit-learn",
  "pyyaml",
  "tqdm",
  "torch"
]

[tool.pytest.ini_options]
testpaths = ["tests"]